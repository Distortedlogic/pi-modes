{
	"name": "pi-modes",
	"version": "0.3.0",
	"private": true,
	"description": "A Pi extension that loads and cycles package and project modes from YAML.",
	"type": "module",
	"keywords": [
		"pi-package"
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Distortedlogic/pi-modes.git"
	},
	"engines": {
		"node": ">=22.19.0"
	},
	"scripts": {
		"check": "npm run typecheck && npm run lint && npm test",
		"lint": "biome check .",
		"test": "node --experimental-strip-types --test test/*.test.ts",
		"typecheck": "tsc --noEmit"
	},
	"pi": {
		"extensions": [
			"./src/index.ts"
		],
		"skills": [
			"./skills"
		]
	},
	"files": [
		"agents.ts",
		"src",
		"AGENTS.yml",
		"README.md",
		"skills"
	],
	"peerDependencies": {
		"@earendil-works/pi-coding-agent": ">=0.85.1 <1",
		"@earendil-works/pi-tui": ">=0.85.1 <1",
		"typebox": "*"
	},
	"dependencies": {
		"pi-agents-yaml": "github:Distortedlogic/pi-agents-yaml#main"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.14",
		"@earendil-works/pi-ai": "^0.85.1",
		"@types/node": "^22.19.0",
		"typescript": "~7.0.2"
	}
}
